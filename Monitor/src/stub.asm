;======================================================================
; 80x86 Stub
; Ferry Hendrikx, April 2024
;======================================================================

cpu 8086

org 0F000h


;======================================================================
; includes
;======================================================================

%include "macro.inc"


;======================================================================
; defines
;======================================================================

cseg                equ 0F000h
dseg                equ 01000h
sseg                equ 07000h

leds_data           equ 08h
uart_ctrl           equ 20h
uart_data           equ 22h


;======================================================================
; stub start
;======================================================================

start:
    cli
    cld

; initialise SS/SP
    mov ax,sseg
    mov ss,ax
    mov sp,0FFFFh

; initialise DS/ES
    mov ax,dseg
    mov ds,ax
    mov es,ax

; welcome
    print mesg_welcome

halt:
    hlt
    jmp halt


;======================================================================
; includes
;======================================================================

%include "messages.inc"
%include "stdio.inc"

